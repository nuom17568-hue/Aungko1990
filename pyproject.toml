[project]
name = "aungko1990-work"
version = "0.1.0"
description = "Small text utility helpers for the Aungko1990 Work project."
readme = "README.md"
requires-python = ">=3.9"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
